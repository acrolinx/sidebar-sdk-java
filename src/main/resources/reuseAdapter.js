(function(){
    window.reuseAdapter = {
        handlePhraseSelection: function(phrase) {
            handlePhraseSelection(phrase)
        },
        log: function(message) {
            logP(message)
        }
    };
})();